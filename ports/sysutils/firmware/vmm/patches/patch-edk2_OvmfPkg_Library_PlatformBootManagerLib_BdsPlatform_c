Use the SerialDxe console for vmd.  vmd already supplies each device's PCI
interrupt line; enable ACPI at its PM1A control port without writing QEMU
PIIX or Q35 routing registers.

Index: edk2/OvmfPkg/Library/PlatformBootManagerLib/BdsPlatform.c
--- edk2/OvmfPkg/Library/PlatformBootManagerLib/BdsPlatform.c.orig
+++ edk2/OvmfPkg/Library/PlatformBootManagerLib/BdsPlatform.c
@@ -222,10 +222,17 @@ PlatformBootManagerBeforeConsole (
   UINT16         FrontPageTimeout;
   RETURN_STATUS  PcdStatus;
   BOOLEAN        FirmwareSetupEnabled;
+  BOOLEAN        SerialConsole;
 
   DEBUG ((DEBUG_INFO, "PlatformBootManagerBeforeConsole\n"));
   InstallDevicePathCallback ();
 
+  SerialConsole = XenDetected () ||
+                  (PcdGet16 (PcdOvmfHostBridgePciDevId) ==
+                   CLOUDHV_DEVICE_ID) ||
+                  (PcdGet16 (PcdOvmfHostBridgePciDevId) ==
+                   VMD_HOSTBRIDGE_DEVICE_ID);
+
   VisitAllInstancesOfProtocol (
     &gEfiPciRootBridgeIoProtocolGuid,
     ConnectRootBridge,
@@ -258,12 +265,11 @@ PlatformBootManagerBeforeConsole (
   // We need to connect all trusted consoles for TCG PP. Here we treat all
   // consoles in OVMF to be trusted consoles.
   //
-  // Cloud Hypervisor doesn't emulate any LPC bridge, which is why it must
-  // rely on the serial I/O port to be connected as a console. It reuses the
-  // definition from Xen as it is very generic.
+  // Cloud Hypervisor and vmd rely on the serial I/O port as their console.
+  // Reuse Xen's generic SerialDxe device path for both platforms.
   //
   PlatformInitializeConsole (
-    (XenDetected () || PcdGet16 (PcdOvmfHostBridgePciDevId) == CLOUDHV_DEVICE_ID) ? gXenPlatformConsole : gPlatformConsole
+    SerialConsole ? gXenPlatformConsole : gPlatformConsole
     );
 
   //
@@ -294,7 +300,7 @@ PlatformBootManagerBeforeConsole (
   // GPU passthrough only allows Console enablement after ROM image load
   //
   PlatformInitializeConsole (
-    XenDetected () ? gXenPlatformConsole : gPlatformConsole
+    SerialConsole ? gXenPlatformConsole : gPlatformConsole
     );
 
   FrontPageTimeout = GetFrontPageTimeoutFromQemu ();
@@ -1339,6 +1345,10 @@ PciAcpiInitialization (
   //
   mHostBridgeDevId = PcdGet16 (PcdOvmfHostBridgePciDevId);
   switch (mHostBridgeDevId) {
+    case VMD_HOSTBRIDGE_DEVICE_ID:
+      // vmd assigns PCI interrupt lines itself.
+      IoOr16 (VMD_PM1A_CNT_IO_ADDRESS, BIT0);
+      return;
     case INTEL_82441_DEVICE_ID:
       Pmba = POWER_MGMT_REGISTER_PIIX4 (PIIX4_PMBA);
       //
